name: 🗓️ Actualizar Calendario FC Barcelona

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - run: pip install requests beautifulsoup4 icalendar pytz
          
      - run: python barcelona-calendar.py
          
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add barcelona.ics
          git commit -m "📅 Update $(date '+%d/%m %H:%M')" || echo "No hay cambios"
          git push
